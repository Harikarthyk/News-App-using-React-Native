import React from 'react';
import HorizontalList from './HorizontalList';

function SportsNews({data}) {
  return <HorizontalList title={'Sports News'} size={22} data={data} />;
}

export default SportsNews;
