import React from 'react';
import HorizontalList from './HorizontalList';

function Entertainment({data}) {
  return <HorizontalList title={'Entertainment News'} size={22} data={data} />;
}

export default Entertainment;
